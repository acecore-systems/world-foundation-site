name: Reconcile Production Vectorize index

on:
  schedule:
    - cron: '7,22,37,52 * * * *'

permissions:
  actions: read
  contents: read

jobs:
  reconcile-production:
    uses: ./.github/workflows/sync-vectorize.yml
    with:
      production_reconcile: true
